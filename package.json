{
  "name": "tfs-slack-bot",
  "version": "1.0.0",
  "description": "customer slack integration with tfs",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dylan dykes",
  "license": "MIT",
  "dependencies": {
    "@slack/client": "^3.16.0",
    "body-parser": "^1.18.2",
    "dotenv": "^5.0.0",
    "express": "^4.16.2"
  },
  "devDependencies": {
    "nodemon": "^1.15.0"
  }
}
